# Architecture Assets

This folder contains public-safe architecture assets for the IT Helpdesk Agent.

- [it-helpdesk-agent-architecture.svg](it-helpdesk-agent-architecture.svg)

The diagram shows the V2 authenticated architecture at a high level. It does not include tenant URLs, endpoint values, subscription IDs, directory IDs, vault URIs, environment IDs, or secret values.
